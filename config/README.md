# Configuration Files

This folder contains build and configuration files.

## Files:
- `craco.config.js` - Custom webpack configuration for production builds

## Note about .env files:
Environment files (.env, .env.development, .env.production) **must remain in the root directory** for Create React App to recognize them. They cannot be moved to this folder.
